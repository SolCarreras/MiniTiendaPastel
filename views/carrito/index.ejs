<%- include("../layout/header", { title: "Tu Carrito" }) %>
   
<section class="carrito-section">
  <div class="carrito-content">

    <h2>🛍️ Tu carrito</h2>

    <% if (carrito && carrito.length > 0) { %>
      <table>
        <thead>
          <tr>
            <th>Producto</th>
            <th>Cantidad</th>
            <th>Precio unitario</th>
            <th>Subtotal</th>
          </tr>
        </thead>
        <tbody>
          <% carrito.forEach(item => { %>
            <tr>
              <td><%= item.producto.nombre %></td>
              <td><%= item.cantidad %></td>
              <td>$<%= item.producto.precio %></td>
              <td>$<%= (item.cantidad * item.producto.precio).toFixed(2) %></td>
                  <td>
          <form action="/carrito/eliminar/<%= item.id %>" method="POST">
            <button type="submit" class="btn-eliminar">Eliminar</button>
          </form>
          
            </tr>
          <% }) %>
        </tbody>
      </table>

      <h3>Total: $<%= total.toFixed(2) %></h3>

    <% } else { %>
      <p>Tu carrito está vacío 🧁</p>
    <% } %>
 
    <a href="/productos" class="btn-ver-productos">Seguir comprando</a>
     <a href="/" class="btn-volver">Volver</a> 
  </div>
</section>

<%- include("../layout/footer", { title: "Footer" }) %>
</body>
</html>
